<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.iyaselerehoboth.theelements">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_periodic_table"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_periodic_table"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".views.activities.HomePageActivity"></activity>
        <activity
            android:name=".views.activities.SplashScreenActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>